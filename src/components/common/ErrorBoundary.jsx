/**
 * Layout and legacy imports: prefer `@/components/ui/ErrorBoundary` with explicit `section` + `variant`.
 */
export { ErrorBoundary as default, ErrorBoundary, withErrorBoundary } from '@/components/ui/ErrorBoundary';
