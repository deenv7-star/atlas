/**
 * SECTION 3 — DEMO DATA CONFIGURATION
 *
 * Success criteria (DONE when all pass):
 * 1. Demo data is clearly labeled when shown (DemoDataBanner)
 * 2. Demo data is never mixed with real user data in the same view
 * 3. Pages using demo: MultiCalendar, DynamicPricing, ExpenseTracker
 * 4. Real data takes precedence when available (future: fetch real, fallback to demo)
 */

export const DEMO_DATA_PAGES = ['MultiCalendar', 'DynamicPricing', 'ExpenseTracker'];
